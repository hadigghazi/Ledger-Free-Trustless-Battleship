[package]
name = "methods"
version = "0.1.0"
edition = "2021"

[build-dependencies]
risc0-build = "3.0"

# Tells risc0-build which guest crate(s) to compile.
[package.metadata.risc0]
methods = ["guest"]
